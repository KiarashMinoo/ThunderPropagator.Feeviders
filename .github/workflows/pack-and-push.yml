name: Pack and Push

on:
  push:
    branches:
      - 'develop'

defaults:
  run:
    shell: pwsh

permissions:
  contents: write

env:
  ArtifactsDirectory: ${{ github.workspace}}/artifacts
  Project_Version: "*.*.^.*"

jobs:
  project_version:
    runs-on: ubuntu-latest
    name: Update version    
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set project versions
        id: update
        uses: vers-one/dotnet-project-version-updater@v1.6
        with:
          file: |
            "**/*.csproj", "**/*.nuspec", "**/AssemblyInfo.cs"
          version: ${{ env.Project_Version }}
      
      - run: |
          git config user.name "Kiarash Minoo"
          git config user.email "yanis_1984@msn.com"
          git add .
          git commit -m "Update project version to ${{ steps.update.outputs.newVersion }}"
          git push
          
  pack_nugets:
    runs-on: ubuntu-latest
    name: Create nuget
    needs: [project_version]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'          

      - run: |
          dotnet restore --configfile nuget.config
          dotnet clean 
          dotnet build -c Debug
          
          dotnet pack ./Feeviders/ActiveMQ/RapidStreamer.Feeders.ActiveMQ/RapidStreamer.Feeders.ActiveMQ.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/ActiveMQ/RapidStreamer.Providers.DotNet.ActiveMQ/RapidStreamer.Providers.DotNet.ActiveMQ.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/Kafka/RapidStreamer.Feeders.Kafka/RapidStreamer.Feeders.Kafka.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/Kafka/RapidStreamer.Providers.DotNet.Kafka/RapidStreamer.Providers.DotNet.Kafka.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/RabbitMQ/RapidStreamer.Feeders.RabbitMQ/RapidStreamer.Feeders.RabbitMQ.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/RabbitMQ/RapidStreamer.Providers.DotNet.RabbitMQ/RapidStreamer.Providers.DotNet.RabbitMQ.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/RedisPubSub/RapidStreamer.Feeders.RedisPubSub/RapidStreamer.Feeders.RedisPubSub.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/RedisPubSub/RapidStreamer.Providers.DotNet.RedisPubSub/RapidStreamer.Providers.DotNet.RedisPubSub.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/TcpSocket/RapidStreamer.Feeders.TcpSocket/RapidStreamer.Feeders.TcpSocket.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/TcpSocket/RapidStreamer.Providers.DotNet.TcpSocket/RapidStreamer.Providers.DotNet.TcpSocket.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/UdpClient/RapidStreamer.Feeders.UdpClient/RapidStreamer.Feeders.UdpClient.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/UdpClient/RapidStreamer.Providers.DotNet.UdpClient/RapidStreamer.Providers.DotNet.UdpClient.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/WebApi/RapidStreamer.Feeders.WebApi/RapidStreamer.Feeders.WebApi.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/WebApi/RapidStreamer.Providers.DotNet.WebApi/RapidStreamer.Providers.DotNet.WebApi.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

          dotnet pack ./Feeviders/WebSocket/RapidStreamer.Feeders.WebSocket/RapidStreamer.Feeders.WebSocket.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      
          dotnet pack ./Feeviders/WebSocket/RapidStreamer.Providers.DotNet.WebSocket/RapidStreamer.Providers.DotNet.WebSocket.csproj -c Debug -o ${{ env.ArtifactsDirectory }}      

      - uses: actions/upload-artifact@v3
        with:
          name: nuget
          if-no-files-found: error
          retention-days: 7
          path: ${{ env.ArtifactsDirectory }}/*.nupkg
             

  push_nuget:    
    runs-on: ubuntu-latest
    name: Deploy to myget    
    needs: [pack_nugets]
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: nuget
          path: ${{ env.ArtifactsDirectory }}
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'   
          source-url: https://www.myget.org/F/rapidstreamer/api/v3/index.json
        env:
          NUGET_AUTH_TOKEN: ${{secrets.MYGET_APIKEY}}

      - name: Publish NuGet package
        run: |
            foreach($file in (Get-ChildItem "${{ env.ArtifactsDirectory }}" -Recurse -Include *.nupkg)) {
              dotnet nuget push $file --api-key "${{ secrets.MYGET_APIKEY }}" --skip-duplicate              
            }

